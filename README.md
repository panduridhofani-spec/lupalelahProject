
  # Export to HTML

  This is a code bundle for Export to HTML. The original project is available at https://www.figma.com/design/ohd4JTTvThEfZLahWDzjXz/Export-to-HTML.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  